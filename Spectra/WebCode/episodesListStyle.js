"""
const list = document.getElementsById('tab-content-episodes');
const episodes = list.getElementsByTagName('ol');
for (const episode of episodes) {
    const id = episode.getAttribute("id");
    if (id.dataset.attr.includes("episodes") {
        id.style.background-color = rgba(0,0,0, 0.5);
        id.style.padding = 24px;
    };
};
"""
